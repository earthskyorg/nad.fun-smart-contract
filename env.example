# Copy this file to ".env" and fill in secrets before running scripts.
# Never commit your personal ".env" file.

# Private key of the deployer account (0x-prefixed). Use a burner for tests.
PRIVATE_KEY=

# RPC endpoints (override defaults in hardhat.config.ts when needed)
RPC_MONAD_MAINNET=https://rpc.monad.xyz
RPC_MONAD_TESTNET=https://testnet-rpc.monad.xyz

# Router used for liquidity migration. Required for `yarn deploy:*`.
UNISWAP_ROUTER_ADDRESS=0x0000000000000000000000000000000000000000

# Optional: enable gas reporter
ENABLE_GAS_REPORT=false


